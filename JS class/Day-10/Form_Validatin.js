function run()

{
    // e.preventDefault();
    let name = document.querySelector('.name').value;
    let userName = document.querySelector('.username').value;

    // console.log(name);
    // console.log(userName);
    window.alert(parseNumber name +userName)
    return false;
    
}