let s = "IloveCoding";
let t=s.indexOf("love") // 1 ye love ka first index batata hai 
console.log(t);
 let p=s.indexOf("J") //-1 (not found)
console.log(p);

 let m = s.indexOf("o") // 2(gives only one index and it prefer to first one , in case 2 is prefered)
console.log(m);
